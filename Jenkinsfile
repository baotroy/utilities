pipeline {
    agent {
        dockerfile true
    }
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Add your build steps here, e.g., compiling code
            }
        }
    }
}
