pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Add your build steps here, e.g., compiling code
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy...'
                // Add your build steps here, e.g., compiling code
            }
        }
    }
}
